export type ScreamType = {
  body: string;
  commentCount: number;
  createdAt: string;
  likeCount: number;
  screamId: string;
  userHandle: string;
  userImage: string;
};

export type ErrorsType = {
  general?: null;
  error?: null;
};
